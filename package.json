{
  "name": "ChemOS",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "start": "tsc && vite build && node server.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/three": "^0.165.0",
    "typescript": "^5.2.2",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "three": "^0.165.0"
  }
}
